
Directories in this "libraries" directory are libraries where
the name of the directory is the name of the library.

Directories may contain the following standard subdirectories:
include/
private/
doc/


